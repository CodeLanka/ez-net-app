import { StyleSheet } from 'react-native';

const TYPE = 'Wave';
const COLOR = '#3F51B5';
const SIZE = 100;

const styles = StyleSheet.create({
  loader: { marginTop: '50%' },
});

export { styles, TYPE, COLOR, SIZE };
